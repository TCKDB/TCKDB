# Playwright follow-up

Playwright is intentionally deferred from this dev-only increment. The Vitest + React Testing Library + MSW suite covers the public shell, identifier routing, and error states without adding browser binaries or a production-serving assumption. Add a single browser smoke once the development proxy and deployment target are defined.
