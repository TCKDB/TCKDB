"""FastAPI route-handler package.

This package is reserved for HTTP resource modules. Pydantic models belong in
``app.schemas``, business logic belongs in ``app.services``, and SQLAlchemy
models belong in ``app.db.models``.
"""
